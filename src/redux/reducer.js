import { EXERCISES } from '../shared/exercises';
//Add contact & login later

export const initialState = {
    exercises: EXERCISES
};

export const Reducer = (state = initialState, action) => {
    return state;
};

